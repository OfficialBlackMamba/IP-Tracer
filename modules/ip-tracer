# Tool Name :- IP-Tracer
# Author :- Sukhpal Kherera
# Date :- 27/05/2024
# Powered By :- Mamba's Software

if [ -d /data/data/com.termux/files/usr/share ];then
  cd /data/data/com.termux/files/usr/share/IP-Tracer
  exec bash trace $@
elif [ -d /usr/share ];then
  cd /usr/share/IP-Tracer
  exec bash trace $@
fi
